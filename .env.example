# URL gốc của API backend.
# Không set thì code fallback về http://localhost:3000/api (xem src/configs/env.config.ts).
# Tạo file .env.local để override cho máy của bạn (đã được .gitignore qua "*.local").
VITE_API_BASE_URL=http://localhost:3000/api
