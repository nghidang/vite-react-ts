# === Môi trường DEV — server dev/tích hợp dùng chung (commit) ===
# Build: yarn build:dev   |   Dev server trỏ API dev: yarn dev:dev
VITE_APP_ENV=dev
VITE_API_BASE_URL=https://api-dev.example.com/api
VITE_ENABLE_DEVTOOLS=true
