# === Môi trường TEST / QA / staging (commit) ===
# LƯU Ý: Vitest chạy ở mode "test" nên cũng nạp file này. Unit test đã mock network
# nên URL ở đây không gây gọi thật — chỉ cần là URL hợp lệ để qua bước validate env.
VITE_APP_ENV=test
VITE_API_BASE_URL=https://api-test.example.com/api
VITE_ENABLE_DEVTOOLS=true
